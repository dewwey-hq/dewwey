# Post-table merge — tick log

Mission: merge `staging.instagram_posts` into `public.posts` (CLAUDE.md thread 0). Plan of record:
`~/.claude/plans/read-thru-my-documentation-reflective-sundae.md` rev 3 (its rev-3 header wins over
the body). One row per tick, appended, never edited. Every tick re-runs the oracle:

```
cd apps/web && bun run scripts/graph/checkPostMergeParity.ts --baseline scripts/graph/snapshots/2026-09-22T22-43-25-093Z-pm-baseline \
  --accepted scripts/graph/tmp_analysis/pm_accepted_w1 --post
```
(`--accepted` = the approved named diffs, CUMULATIVE: `pm_accepted_pm-20260923-merge-1` after P3 (tick 8), `pm_accepted_w1` after W1 (tick 9) — always use the latest; `--post` asserts the post-merge
post-conditions. Before tick 8 the command had neither flag.)

The baseline (rows in gitignored `snapshots/`, summary in
`apps/web/scripts/graph/tmp_analysis/pm_baseline_2026-09-22T22-43-25-093Z.json`) is written ONCE and
never rewritten; a diff is resolved by naming its cause.

**Writer lock: OFF since tick 12 (P4-W3 committed 2026-09-23).** (It was ON from tick 4, P1 applied ~03:45 UTC.) While ON: no acquisition
tick, parse, reader, creation or revert run.

| tick | phase | what | counts / parity | named causes | lock |
|---|---|---|---|---|---|
| 1 (2026-09-22 ~23:00 UTC) | P0 | Built `checkPostMergeParity.ts` (18 frozen outputs as full-row multisets + 3 captured sets + post-conditions; funnel SQL copied from `reportCorpusInventory.ts`). Wrote the one-time baseline `2026-09-22T22-43-25-093Z-pm-baseline`. Extended `snapshotGraphTables.ts` with `ops.post_observations`, `ops.crawl_runs`, `stack_extraction_runs`, `post_venue_verdicts`, `human_post_labels`, `jeremy_wedding_candidate_posts`. | Self-check: **0 frozen diffs, 0 failures** (393 s). Baseline: structural 62,139 rows · corpus urls 67,874 · legacy-unobserved 3,610 (0 in structural) · profile urls 10 · evidence orphans 0 across 15 url-keyed tables · posts 26,918 = distinct shortcode = distinct url · observations: 8,856 posts unobserved, 1,322 with zero `is_first`, 0 with several. | First self-check showed a false `v1_content_corpus` diff: captions contain newlines and the baseline file is newline-joined. **Fixed in the reader, not by re-baselining.** Continuation lines are regrouped and PROVEN against the row count + sha256 stored at write time; any mismatch throws. Findings for later ticks: (a) the DB is **971 MB** while CLAUDE.md says free tier (500 MB cap), so confirm the plan before P3; the merge adds about the size of staging. (b) Storage: to avoid holding every staging row twice, P1 will put the staging row in `raw` (`raw_format='jeremy_staging_v1'`) for staging-only and `jeremy_evidence` rows, and in `staging_raw` only where `raw` is an Apify payload (the 1,318). This is the same rev-3 semantics (staging precedence via `staging_post_id`) with no duplication. | OFF |
| 2 (2026-09-22 ~23:10 UTC) | P0.5 | Writers made safe for both layouts (code only, no DB write). New `scripts/graph/postMergeCompat.ts` (`postsHasOrigin`, a cached `information_schema` check). `acquire/ingest.ts` states `origin='acquisition_loop'` once the column exists (source was already explicit; `scraped_at=now()` is the true clock there). `createWeddingsFromJeremyEvidence.ts`: all 4 staging-copy sites go through `JEREMY_POST_COLS/VALS`, which gain `origin='jeremy_beta'` once the column exists. The copy path stays until P3, and after P3 `on conflict do nothing` links the existing row. `revertWeddingBatch.ts` **no longer deletes posts**: orphans are printed as `posts_left_unattached`, and `removed_posts_imported` is logged empty. `pipeline/pipeline.py` states `source='venue_tagged'` explicitly and `origin='ben_pipeline'` once the column exists. | Parity: **0 frozen diffs, 0 failures** (382 s). tsc clean on the touched files; `scripts/acquire` vitest 99/99. `pipeline.py` syntax-checked only (no Python packages in this sandbox). | **Deliberately NOT in P0.5:** the reader filters on `source='jeremy_evidence'` (revert's orphan/mixed detection, `acquire/targets.ts`, `reportCorpusInventory.ts`). Those values change in P3, so the filters change with them (rev 3 item 2) in P4 W3/W4. Every script that runs them is under the writer lock during that window. | OFF |
| 3 (2026-09-22 ~23:25 UTC) | P1 (NOT applied) | Wrote `applyPostMergeSchema.ts` (23 idempotent statements + 7 pre-COMMIT assertions; `--print/--dry-run/--apply`; `--apply` refuses without a snapshot < 24h). Snapshot taken: `2026-09-22T23-08-24-417Z-pre-pm-p1-schema` (12 tables). The creation copy path now also states `raw_format='jeremy_evidence_subset'`. **Finding that changed the origin rule:** run 31 `legacy-ben-crawl1-zero-venues` (D061, the user's call) already registered 1,541 of Ben's posts as sightings, and 826 Ben posts were later re-seen by the loop. So "has an observation" ≠ "the loop created it". Origin is now derived two ways (provenance and the load clock), and the apply refuses to commit on any disagreement. **Consequence for P3:** the structural gate must be "staging-linked OR observed by a non-`legacy-import` run", not `origin='acquisition_loop'`, or the 1,541 run-31 posts would drop out. | Dry-run 1: origin acquisition_loop 15,199 (own_profile 145 + venue_tagged 15,054) · ben_pipeline 6,370 · jeremy_beta 5,349; **0 disagreements** between the two derivations; sentinel unchanged; synthetic runs outside `acq-%`; url unique. 1 assertion failed: post 37676 (`DaAoG40mUmC`, copied 2026-09-21 via the `v_ig_posts` path) carries an extra `corpus_source` key in `raw`. Named, and the assertion was narrowed to allow that tag. Rolled back. **Dry-run 2 could not start: the DB is now READ-ONLY** (`default_transaction_read_only=on` at system level, error 25006). DB 974 MB. Verified no P1 state persisted (0 new columns, 0 synthetic runs, no log table; autovacuum cleared the rolled-back tuples at 23:20). | **STOP: new finding outside the plan.** Supabase has put the project in read-only mode, very likely its disk-limit enforcement (CLAUDE.md says free tier, capped at 500 MB, and the DB was already 971 MB). Dry-run 1 rewrote all 26,918 `posts` rows and built 2 unique indexes before rolling back, which generates WAL/disk churn and may have tipped it over. This blocks ALL writes, including the app's (`/label`), not just this loop. Needs the user: raise the disk/plan, or decide a cleanup. The P3 merge adds about 100+ MB, so it must not proceed on this disk either way. | OFF |
| 4 (2026-09-23 ~03:45 UTC) | P1 **applied** | Resumed after the user moved the project to Supabase Pro (transfer into a new org, same ref and `DATABASE_URL`; `default_transaction_read_only=off`, DB 975 MB). Pre-check parity 0 diffs (410 s). Snapshot `…23-08-24-417Z-pre-pm-p1-schema` (~4.5 h old). Dry-run green, then `--apply` COMMITTED with all 7 assertions re-checked before COMMIT. DDL mirrored into `pipeline/schema.sql` ("POST-TABLE MERGE (P1, 2026-09-22)" banner, transcribed from `STATEMENTS`). `reportCorpusInventory.ts` notes why only `acq-%` batches are ranked. | posts: origin **acquisition_loop 15,199** (own_profile 145 + venue_tagged 15,054) · **ben_pipeline 6,370** · **jeremy_beta 5,349**; raw_format apify_v1 21,569 · jeremy_evidence_subset 5,349. Synthetic runs **91 `legacy-ben-pipeline`**, **92 `legacy-jeremy-beta-import`** (actor `legacy-import`, 0 observations yet). Identity ids 89/90 were consumed by the rolled-back dry-runs, which is harmless. Post-apply parity: **0 frozen diffs, 0 failures** (408 s). | none new | **ON** |
| 5 (2026-09-23 ~04:40 UTC) | P2 (in progress — no packet approved) | Built `mergeStagingPosts.ts`: ops in one transaction, the frozen/post-condition checks run inside it, and a markdown gate packet written to `tmp_analysis/pm_gate_packet_<batch>_dryrun.md`. `checkPostMergeParity.ts` now exports its building blocks (`baselineLoader`, `FROZEN`, …); behaviour unchanged. **Dry-run 1** was cancelled after the structural view ran >19 min (vs ~50 s): single-branch `v_ig_posts` with `corpus_source` as a CASE gave the planner no statistics. Fixed with two UNION ALL branches on `staging_post_id` + `ANALYZE` inside the transaction; now 47 s. **Dry-run 2** completed and rolled back. | Dry-run 2: exclusions 10 · minted **1,409** · twins **5** (7 posts) · inserted **40,946** · relabeled 5,349 · linked 1,318 · field conflicts 2 (DcOQaPtC6bk caption 773/767; C__71R1P-1_ owner) · sightings 47,613 (run 94) + 4,829 (run 93) · is_first set on 51,124 · posts after = 67,864 = distinct shortcode = distinct url · funnel only total −10. Frozen diffs, each a named cause: `human_confirmed_post_vendor_association` −32/+32 and `…vendor_page_content` −6/+6 (author_account_id NULL → a **minted** account); `post_styled_shoot_signal` −5/+5 (the **twin default**: author becomes the existing account's handle); structural view −15/+15 and batch fns −4/+4, −2/+2 (**pre-existing nondeterministic venue tie**: `credit_line_venue` DISTINCT ON has no tie-break, two venue handles on one line, all rows `venue_anchor_conflict=t`; baseline view and function already disagreed on DcWbbHFmyph). Orphans 10/10/1/1/1 = the profile urls (now allowed via `ops.post_merge_exclusions`). "New" posts the loop counted though staging held them: d065scale 306, d065C 67, d065D 64, probesA 63 … (reported, not rewritten). | Fixes added for dry-run 3: lowest-account-id tie-break in the view and function, a -/+ pairing explainer, and an exclusion-aware orphan check. **Dry-run 3 could not start: the DB is READ-ONLY AGAIN** (1,090 MB + 384 MB WAL; the Pro transfer evidently kept a small provisioned disk). Nothing persisted (posts 26,918). **STOP: infra.** Needs the user: raise the disk in the Supabase dashboard (Project Settings → Compute and Disk), then VACUUM before the next dry-run. | **ON** |
| 6 (2026-09-23) | P2 **gate packet ready** | DB writable again. Plain `VACUUM (ANALYZE)` on posts / ops.post_observations / accounts / merge tables made the rolled-back dry-runs' dead space reusable (posts 307 MB on disk, ~100 MB of it free; no VACUUM FULL). Dry-run 4 with the tie-break + pairing explainer. | Same ops as dry-run 2. **Verdict: 0 unexplained frozen rows, 0 post-condition failures.** Funnel: only total 67,874 → 67,864 (the 10 profile urls). Frozen diffs, all named: structural −109/+109 and batch fns −5/+5, −4/+4 = the deterministic venue tie-break (lowest account id; every row `venue_anchor_conflict=t`); association −32/+32 and vendor_page −6/+6 = minted accounts resolve; styled-shoot −5/+5 = the twin default. DB 1,090 → 1,186 MB after the dry-run (WAL), still writable. | **STOP — waiting on the user's word:** (1) approve the P3 + P5 writes, (2) the five twin decisions, (3) raise the Supabase disk before P3 (P3 writes the same volume once more). Packet: `apps/web/scripts/graph/tmp_analysis/pm_gate_packet_pm-20260923-merge-1_dryrun.md`. | **ON** |
| 7 (2026-09-23 ~05:40 UTC) | P3 **applied** | User approved P3+P5 and chose **existing account for all 5 twins** (`tmp_analysis/pm_twin_decisions_pm-20260923-merge-1.json`). The Supabase disk auto-scaled 2 GB → 8 GB (Pro). Snapshot `2026-09-23T05-16-25-799Z-pre-pm-p3-merge`. `mergeStagingPosts.ts --apply` **COMMITTED** after the same in-transaction verdict as the gate packet (0 unexplained, 0 failures). Packet: `tmp_analysis/pm_gate_packet_pm-20260923-merge-1.md`. | Fresh-connection check: posts **67,864** = distinct shortcode = distinct url; origin/source: acquisition_loop 15,199 · ben_pipeline 6,370 · jeremy_beta own_profile 42,276 + unknown 4,019. `v_ig_posts` rows = distinct = 67,864. Merge log: insert 40,946 · mint 1,409 · relabel 5,349 · link 1,318 · is_first_set 51,124 · field_conflict 2; exclusions 10. is_first: 0 posts with none, 0 with several. DB 1,186 MB on an 8 GB disk. **Point of no return:** the revert SQL in the packet stays valid until the lock lifts at P4-W3. | Accepted named diffs (the packet lists every row): venue tie-break (structural −109/+109, batch fns −5/+5 and −4/+4), minted accounts resolving (−38/+38), twin default (−5/+5), funnel total −10. | **ON** |
| 8 (2026-09-23 ~06:30 UTC) | P4 prep | The post-P3 standalone parity showed EXACTLY the approved named diffs (structural −109/+109, batch fns −5/+5 and −4/+4, styled −5/+5, association −32/+32, vendor_page −6/+6, funnel −1/+1) and 0 post-condition failures. `checkPostMergeParity.ts` gained `--record-accepted <dir> --expect …` (recorded once, refuses if the dir exists or the counts differ from the packet) and `--accepted <dir>` (expected = baseline − accepted.minus + accepted.plus). Recorded `tmp_analysis/pm_accepted_pm-20260923-merge-1/accepted.json`. The baseline was not touched. | `--accepted --post` parity: **0 frozen diffs, 0 failures** (410 s). This is the gate for every P4 wave. | none | **ON** |
| 9 (2026-09-23 ~07:30 UTC) | P4 **W1 applied** (DB layer) | `applyPostMergeViewsW1.ts --apply` COMMITTED. New view **`v_jeremy_beta_posts`**: posts rows linked to staging, exposing the staging columns with their original types, plus post_id/shortcode. The 7 views that read staging (v1_content_corpus, human_confirmed_post_geography, …_post_vendor_association, venue_couple_signal_…, venue_inline_mention_…, venue_portfolio_content, post_styled_shoot_signal) now read it. **No DB object depends on `staging.instagram_posts` any more** (checked via pg_depend in the transaction). Live post-merge DDL appended to `pipeline/schema.sql` under a "POST-TABLE MERGE (P3 + P4 W1)" banner that supersedes the earlier definitions; the block was validated by executing it in a rolled-back transaction (the first attempt's awk splitting corrupted a function body; regenerated from the script). | Column-by-column equality vs staging for every column and row of `v_jeremy_beta_posts` and the `v_ig_posts` staging branch: **0 differences**. Frozen: all identical except `post_styled_shoot_signal` −10 (the profile urls, named). Cumulative accepted diffs recorded as `tmp_analysis/pm_accepted_w1` (styled now −15/+5). | **Found and fixed a latent P3 bug:** `to_jsonb()` turns SQL NULL into JSON null, so the P3 `v_ig_posts` returned jsonb `'null'` for 25,107 staging mentions and 17,633 hashtags. No frozen output exposed those columns; code doing `jsonb_array_elements(coalesce(mentions,'[]'))` would have thrown. Now `NULLIF(…, 'null'::jsonb)`, and the new column-equality check would catch any recurrence. **Carried to W5:** the old `apply*Schema.ts` scripts still contain the pre-merge staging-reading view text; re-running one would regress a view. The lint guard must flag them. | **ON** |
| 10 (2026-09-23 ~06:55 UTC) | P4 **W2 server code committed**; W2 view speed-up written, NOT applied | Server: `lib/server/graph.ts` (/weddings + vendor feed media type), `labeling.ts` (label queue), and `postVenueReview.ts` (spot-check queue, progress, 3 dead fallbacks) read `v_jeremy_beta_posts` instead of staging. The 3 `distinct on (shortcode) … from v_ig_posts` wrappers were dropped: redundant now (one row per post) and they forced a ~1-min full sort per page load. Test fixture `labeling.test.ts` moved off staging. **Page content checked before/after** (captured from the dev server, visible text diffed): `/vendors/foxandivory` p1+p2, `/label/candidates?post=CfT6u3dufqb` (a Jeremy post), `?post=DJpEclux7Zz` (a crawled post), `/label/candidates`, `/label`: **0 text differences on all 6**. | tsc clean. **Performance finding, root-caused:** the P3 `v_ig_posts` (and the W1 `v_jeremy_beta_posts`) pick the staging row via `CROSS JOIN LATERAL`, which stops the planner flattening the UNION ALL, so a post_url lookup hashes all ~68k rows. `/label/candidates?post=` still takes 16–23 s (the query alone ~17 s). The fix is `applyPostMergeViewsW2.ts`: the same views as 3 plain UNION ALL branches split on raw_format/staging_post_id, gated on column-equality vs staging + all frozen outputs. **Its dry-run could not start: the DB is READ-ONLY AGAIN** (1,186 MB + 256 MB WAL, on the disk Supabase said was resized to 8 GB). Nothing persisted. | **STOP: infra (3rd time).** This time it doesn't look like a plain full disk; the user must check the Supabase dashboard (disk usage graph, resize status, any read-only banner/reason). Someone was labeling on `/label/candidates?batch=…` at the time; their verdict writes will fail while read-only. When writable: run `applyPostMergeViewsW2.ts --dry-run`, then `--apply`, re-time the page, then W3. | **ON** |
| 11 (2026-09-23) | P4 **W2 complete** | DB writable again (the user checked Supabase; cause of the 3rd read-only unknown, DB 1,186 MB, WAL 224 MB). `applyPostMergeViewsW2.ts --apply` **COMMITTED**: `v_ig_posts` and `v_jeremy_beta_posts` are now plain UNION ALL branches split on raw_format/staging_post_id, with no LATERAL. The direct-open review query also filters the view by its own urls (`where post_url = any($1)`), so the filter reaches every branch. | Gate inside the transaction: column-by-column equality vs staging = 0 diffs; `v_ig_posts` 67,864 rows = 67,864 distinct; `v_jeremy_beta_posts` 47,613 = staging's real posts; all frozen outputs = baseline + `pm_accepted_w1`. Single-url lookup: ~17 s → **14 ms**, 0 seq scans. `/label/candidates?post=CfT6u3dufqb` 18–25 s → **4–6 s**, with visible text identical to the pre-W2 capture (both posts). | Remaining page time is outside the merge's code paths (hydration etc.); not chased. | **ON** |
| 12 (2026-09-23) | P4 **W3 committed, writer lock LIFTED** | A Sonnet builder repointed the pipeline readers (runStackParserBaseline, runStackParserV10, classify/source, runExtract, runJeremyWeddingClustering, createWeddingsFromJeremyEvidence, revertWeddingBatch, acquire/targets): `staging.instagram_posts` → `v_jeremy_beta_posts`; redundant `distinct on` wrappers over `v_ig_posts` dropped; `source='jeremy_evidence'` → `origin='jeremy_beta'`; the creation copy path always states origin/raw_format and uses source `unknown`. **The builder stopped on `targets.ts`'s vendor-yield CTE and was right:** since P3 it matched 0 posts, so every vendor's own_yield was 0 and the vendor tier returned nothing. Replaced with `origin='jeremy_beta' and exists(wedding_posts)`, which is **exactly the old 5,349-post set** (0 difference either way, checked against the merge log's before-state). | **Old-vs-new proof:** each dry-run ran with the pre-W3 code (worktree @946afdb) and the new code against the same DB, then outputs were diffed. Identical: parser default (5,225 posts, same stats), parser ungated, parser v10, extract corpus, extract pool-b (420 lines), clustering structural (4,538 eligible), creation `--from-confirmed-candidates` (403 eligible, 413 lines). Different by design: `targets --tier vendor` old 0 → new 2 tier-A vendors (the fix). tsc clean. | **ROOT CAUSE of the "read-only" episodes found — it was us.** `default_transaction_read_only=on` had `pg_settings.source='session'` on ONE pooled backend: my own read-only checks ran `psql -c "set default_transaction_read_only=on;" …` against the transaction-mode pooler (:6543), and the session SET stuck to the backend and was served to every later client (scripts, app, a human labelling). Fixed with `set … = off` via the pooler; every backend then showed `off`/`default`. The earlier free-tier/disk explanations were wrong or at best partial (the Pro upgrade is still justified at 974 MB). Rule: never a bare SET via :6543; use `begin read only` / `set local`; if it "goes read-only", check `pg_settings.source` first. | **OFF** |
| 13 (2026-09-23) | P4 **W4 committed** (reports/queues) | A Sonnet builder: (A) swapped SQL reads of `staging.instagram_posts` → `v_jeremy_beta_posts` in 33 files (queue builders, reports, classify, graph scripts, app/label, lib/feedDesign); (B) put a refuse-to-run guard (`POST_MERGE_SUPERSEDED` marker; refuses when `v_jeremy_beta_posts` exists) on the 8 superseded `apply*Schema.ts` scripts whose DDL would point views back at staging; (C) rewrote `reportCorpusInventory.ts`'s provenance section: posts by origin × source, **import-record reconciliation** (the one allowed staging read, marked), and sightings by channel. | Parity (start of tick, post-W3): **0 frozen diffs, 0 failures**. tsc clean. Remaining `from/join staging.instagram_posts`: only the guarded apply scripts, the merge/parity tooling, and the marked reconciliation. `reportCorpusInventory --batches`: total **67,864**; staging 47,623 = linked 47,613 + excluded 10 **OK**; channels: acquisition_loop 87 runs / 15,199 first-observed, legacy-ben-pipeline 4,534, legacy-jeremy-beta-import 46,590, run 31 1,541; funnel parsed 99.0% · clustered 21.4% · in a wedding 13.4%. Smoke-ran reportVendorAssociation, reportNeverSeenVenueGeography (OK). | **Named perf follow-up:** the `human_confirmed_*` views run 1.5–2.5× slower post-merge (association 6→17 s, evidence 3→6 s, vendor_page 25→43 s, per parity timings) because Jeremy's fields come out of jsonb. Outputs are identical. `reportHumanConfirmedGraphValue.ts` step 6 now exceeds the role's default statement_timeout. Fix options for later: typed columns on posts for the hot staging fields, or `set local statement_timeout` in that report. Not a blocker. | OFF |
| 14 (2026-09-23) | P4 **W5 committed** (guards + tests) + page-performance fixes | **New `scripts/graph/postMergeInvariants.test.ts` (10 tests, green).** Lint: no FROM/JOIN of `staging.instagram_posts` outside an explicit allowlist (merge tooling, the marked reconciliation, `POST_MERGE_SUPERSEDED` scripts); no SQL filter on `source='jeremy_evidence'`. DB: count = distinct shortcode = distinct url = `v_ig_posts` rows; every post has ≥1 sighting and exactly one is_first; no `jeremy_evidence`, no null origin; staging = linked + excluded; no view/function body reads staging (comment lines ignored); core evidence tables have 0 orphans; **`pipeline/schema.sql`'s POST-TABLE MERGE block == live definitions for all 11 objects**. The block was regenerated from live, because the W1 block was stale after W2. `graphStrengthening.test.ts`: the structural-gate test is rewritten to the real gate (a non-`legacy-import` sighting), and the jeremy_evidence test moved. | **Two page regressions found by the tests and fixed:** (1) `graph.ts` (vendor feed) joined `v_jeremy_beta_posts` per wedding, and after W2 (plain UNION ALL) that was a full view scan per wedding, **60–90 s per vendor page**, which starved the app's 5-connection pool (so `/label` timed out too). It now reads post_type/media dims straight off the posts row (raw/staging_raw); `labeling.ts` does the same (2.7 s → fast). (2) `postVenueReview.ts` queue paths: a plain join hash-joined the whole view and de-toasted every payload (~12 s warm). Now `cross join lateral (… where v.post_url = cp.source_post_url offset 0)`, index scans, ~0.5 s. A LATERAL without the OFFSET 0 fence gets flattened back (tried; 12 s; reverted). Results: `/label/candidates` 33 s → **7.4 s**, direct-open 18–25 s → **0.4 s**, `/label` **0.35 s**, vendor page ~5–7.5 s; **visible text identical to the pre-W2 captures on all pages**; labeling + postVenueReview tests 5/5. | Lesson: after any view change, re-time EVERY page that joins it (tick 11 re-timed only the candidates page and missed the vendor feed). | OFF |
| 15 (2026-09-23) | P5 **lock-down applied** | `applyPostMergeLockdown.ts --apply` COMMITTED after a dry-run. FKs to `posts(url)` added NOT VALID then **VALIDATED** on stack_extraction_runs, post_extraction_runs, post_venue_verdicts and jeremy_wedding_candidate_posts. `human_post_labels` FK is **NOT VALID on purpose**: one legacy human label (jeremy, 2026-09-05, NOT_WEDDING) is on `chicagocatz/`, a profile url that was never a post; labels are append-only, and every new label is enforced. `staging.instagram_posts` is now **read-only by trigger** (INSERT/UPDATE/DELETE + TRUNCATE; a REVOKE would do nothing since only the owner holds grants) and COMMENTed as the import record. Mirrored to `pipeline/schema.sql` (P5 block after a new `-- END POST-TABLE MERGE VIEWS` marker). | In-transaction probes: an update on staging is rejected; an evidence insert for a non-post url fails with **23503** (the first probe accepted any "violates" message and would have passed on a NOT NULL error; tightened to the FK code). Invariants **11/11** (new: P5 FKs + triggers pinned). The lint flagged the P5 script's own staging write-probe, correctly; it was allowlisted as merge tooling. Parity: see the next row. | none | OFF |
| 16 (2026-09-23) | P6 **truth layer applied, regression set v1 frozen** | Post-P5 parity (tick 15): **0 frozen diffs, 0 failures** (501 s). `applyPostTruth.ts --apply` COMMITTED. **`post_truth`**: one row per post; label wedding/not_wedding/unknown/conflicting × tier gold (human label or human verdict, latest per post; human beats model) / silver (latest model verdict incl. fable-structured, then retirement, then wedding attachment) / bronze (candidate cluster only). `label_source`, `sources` jsonb, `retirement_candidate`, `silver_disagreement`. The human/model split is computed per source, because `post_venue_verdicts_current` is latest-across-reviewers and would hide an earlier human verdict under a later model/revert row. **`eval_set_versions` / `eval_set_members`**: insert-only by trigger. **v1 `post-truth-regression-v1`: 4,316 members, sha256 `83879f9f…6276`**; the 6 suspected label slips are flagged, not dropped; called a *regression* set, not a holdout. New read-only `reportStrategyYield.ts`: posts → gold/silver/bronze weddings per first-sighting channel/batch, W/$ for acq-% strategies only. Mirrored to `pipeline/schema.sql` (validated in a rolled-back transaction). | Gold 4,319 posts: wedding 2,593 · not_wedding 1,723 · **conflicting 3**. Silver: wedding 7,732 · not_wedding 1,625. Bronze 3,912. Unknown 50,276. Gold by origin (W / not-W / conflicting): jeremy_beta 2,046 / 1,383 / 0 · acquisition_loop 412 / 152 / 3 · ben_pipeline 135 / 188 / 0. Strategy yield (gold+silver W/$): canary-vendor 113.8 · vendor 99.2 · pilot 98.3 · d065C 98.2 … probesB 7.3 · d065A 6.1; Jeremy's own-profile channel 0.138 W/post vs 0.25–0.58 for the best strategies. Invariants **12/12** (v1 sha pinned). | **Finding for the user: 74 retirement candidates**, i.e. posts a human labelled NOT_WEDDING that are still attached to a wedding (`select * from post_truth where retirement_candidate`). Listed, not acted on. | OFF |
