node_modules/
.next/
.env
.env.local
.env*.local
__pycache__/
*.pyc
.DS_Store
avatars/
backups/
*.log
state.json
.vercel
secrets/

# agent tooling — local state only; .claude/skills/ is committed
.claude/settings.local.json
.claude/worktrees/
.cursor/

# playwright-mcp session artifacts
.playwright-mcp/

# D055 provenance snapshots (apps/web/scripts/graph/snapshotGraphTables.ts) -- large gzipped CSV
# dumps of graph tables, local safety net only, never committed
apps/web/scripts/graph/snapshots/
