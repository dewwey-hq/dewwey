/** Page width — a bit wider than max-w-7xl (80rem / 1280px). */
export const SITE_MAX_WIDTH_CLASS = "max-w-[88rem]";

/** Horizontal padding — slightly tighter than px-4 sm:px-6 lg:px-8. */
export const SITE_PADDING_X_CLASS = "px-4 sm:px-5 lg:px-6";

export const siteContainerClass = `mx-auto w-full ${SITE_MAX_WIDTH_CLASS} ${SITE_PADDING_X_CLASS}`;

/** Shared primary nav bar height. */
export const SITE_HEADER_HEIGHT_CLASS = "h-[4.75rem]";
